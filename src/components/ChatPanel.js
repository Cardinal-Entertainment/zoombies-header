import React, { Component } from "react";


class ChatPanel extends Component {
    render () {
        return (   
            <div id="chat-panel">
                Hello from chat
            </div>
        );
    };
};

export default ChatPanel;